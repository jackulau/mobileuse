# Add task-specific helpers here. Anything you define gets injected into
# `mobile-use -c` / `iphone-harness -c` / `android-harness -c` script globals.
# Empty by default. Underscore-prefixed names are not exported.
